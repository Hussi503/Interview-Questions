1. How do you implement cross-account access in Amazon Web Services using IAM roles?
2. What is the recommended folder structure for a production-grade Terraform project?
3. How do you call a Terraform module from another module or root module?
4. When designing a VPC architecture, should a Load Balancer be placed in a public subnet or private subnet? Why?
5. How do you restrict pod-to-pod communication in Kubernetes inside a cluster?
6. What steps would you take if a Kubernetes pod is stuck in CrashLoopBackOff?
7. How do you debug a failed CI/CD pipeline in GitLab? What are the first things you check?**
8. How do you securely store and manage secrets in Kubernetes or cloud environments?
9. How do you design a rollback strategy for a failed production deployment?
10. If two engineers run Terraform apply at the same time, what problem can occur and how do you prevent it?
11. Production application is inaccessible after deployment. How will you troubleshoot?
12. One Kubernetes node suddenly goes down. What happens and how do you recover?
13. Terraform apply failed after creating some resources. What steps will you take?
14. ALB is up but the application is not reachable – how do you troubleshoot?
15. EC2 instance is running but unreachable – what steps do you follow?
16. EKS pods are restarting frequently – how do you debug?
17. CI/CD pipeline is failing after Terraform apply – what do you check?
18. AWS region goes down – how do you recover your application?
19. CloudWatch shows high CPU but low traffic – what could be the issue?

1. Jenkins pipeline failed while creating AWS resources – what do you check first?
2. Terraform apply failed in Jenkins but works locally – why?
3. EC2 created but provisioning failed – how do you debug?
4. How do you rollback a failed deployment?
5. How do you make CI/CD pipelines idempotent?


. What happens if two engineers run terraform apply at the same time?
• Terraform uses state locking (remote backend like S3 + DynamoDB).
• One apply proceeds, the other fails to acquire the lock.
• Without locking, state corruption and duplicate resources can occur.

2. Why does Kubernetes show Pods as Running but users still face issues?
• Pod status only shows container health, not application health.
• Possible causes: failed readiness probes, network issues, downstream • dependency latency, or partial AZ failure.
• This is why readiness checks and observability matter.

3. What causes CI/CD pipelines to fail intermittently?
• Flaky tests
• Dependency version drift
• Shared runners or resource exhaustion
• Race conditions in parallel jobs
Stable pipelines require determinism, not retries.

4. How do you prevent unnecessary redeployments in CI/CD?
• Use proper change detection (path-based triggers).
• Separate build, test, and deploy stages.
• Avoid rebuilding artifacts when inputs haven’t changed.

5. Why is infrastructure drift dangerous?
• Manual console changes break IaC assumptions.
• Terraform may try to “fix” production unexpectedly.
• Drift leads to unpredictable applies and outages if not detected early.

6. How do you safely share secrets across environments?
• Never hardcode secrets in repos or Terraform state.
• Use secret managers (Vault / cloud-native services).
• Inject secrets at runtime with strict access control.

7. What’s the difference between liveness and readiness probes?
• Liveness decides when to restart a container.
• Readiness decides when traffic should be sent.
• Misconfigured probes cause traffic drops or restart loops.

8. How do you debug high latency when CPU and memory look normal?
• Look at p95/p99 latency, not averages.
• Check network, retries, timeouts, and downstream services.
• Infra health ≠ user experience.

9. When should you scale vertically vs horizontally?
• Horizontal scaling improves availability and resilience.
• Vertical scaling is simpler but has limits and restart impact.
• The choice depends on statefulness and failure tolerance.

10. What’s the biggest mistake engineers make during outages?
• Restarting things without understanding the blast radius.
• Fixing symptoms instead of isolating root cause.
• Making changes without rollback plans.



REAL SCENARIO QUESTIONS (The "We Actually Fired Someone For This" Ones)
1. "It's 2 AM. PagerDuty wakes you up. Production database is down. All developers are asleep. You have 15 minutes to fix or company loses $1M per hour. What do you do?"
2. "A developer accidentally deleted the entire /etc directory on a critical server. No backups. How do you recover?"
3. "Your CI/CD pipeline deployed to production. Now customers can't login. The last good deployment was 3 hours ago. 5000 transactions lost in that time. How do you handle this?"
4. "Your manager says 'We need to move everything to Kubernetes by next month.' The team has no K8s experience. What's your plan?"
5. "Security team found a critical vulnerability in base image. 500 microservices use it. How do you remediate in 24 hours?"
6. "AWS bill doubled this month. No one changed anything. Where do you start investigating?"
7. "Developer says 'It works on my machine' but fails in dev environment. How do you respond and resolve?"
8. "Your Terraform state file was accidentally deleted from S3. Local copies are 2 days old. How do you recover?"
9. "Kubernetes cluster is completely unreachable. API server down. etcd might be corrupted. You have 1 hour to recover production."
10. "Someone committed AWS keys to public GitHub. They've been there for 3 months. What's your incident response?"
